# Autogenerated by terraform, run terraform apply after updating the template
# terraform apply -target local_sensitive_file.ansible_inventory

all:
  hosts:
    wordpress:
      ansible_host: ${wordpress_public_ip}
      ansible_user: ubuntu
      ansible_python_interpreter: /usr/bin/python3
      wordpress_host: wordpress.abz.prooton.ee
      wordpress_docker_image: ghcr.io/kristianheljas/abz-wordpress:1.0.0
      wordpress_admin_email: kristian@kristian.ee
      wordpress_admin_password: "${wordpress_admin_password}"
      wordpress_db_host: "${wordpress_db_host}"
      wordpress_db_root_password: "${wordpress_db_root_password}"
      wordpress_db_password: "${wordpress_db_password}"
      wordpress_redis_host: "${wordpress_redis_host}"
      wordpress_redis_password: "${wordpress_redis_password}"